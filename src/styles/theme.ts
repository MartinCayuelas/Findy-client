import { DefaultTheme } from 'styled-components'

const gray = '#F3F4F5'
const primary = '#001D30'
const success = '#00CFA4'
const white = 'white'
const secondary = '#341E70'
const radiusCard = '8px'

export const defaultTheme: DefaultTheme = {
  gray,
  primary,
  radiusCard,
  secondary,
  success,
  white,
}

export const exampleTheme = {
  colorText: 'red',
}
